# 변경 이력 (CHANGELOG)

> 사람이 한눈에 보는 주요 변경 누적 기록. 최신이 위.
> 상세 코드 변경은 `git log`(커밋 메시지)에 영구 기록됨.
> **새 작업을 하면 이 파일 맨 위에 한 줄 추가하는 것을 규칙으로 한다.**

---

## 2026-06-22

### 급여 체계 개편
- `STAGE_PAYROLL_MAX`(단계별 최대 일수) 제거 → **학생당 전체 6회 / 하루 2회 / 30분 쿨다운** 통합 관리
- 업무일지(worklog) 없으면 급여 지급 불가 (KST 기준 오늘 것)
- 직원·공무원 모두 `payroll:` 접두사로 통일 → 개인별 합산 카운트 정확
- 반 간 수업일 차이 관계없이 최대 수령액 동일 (형평성 확보)

### 가이드 문서 추가
- `docs/교사용_가이드매뉴얼.md` — 단계별 운영 방법, 학급비번, FAQ, 수업 흐름도
- `docs/학생용_가이드매뉴얼.md` — 역할별(CEO/직원/공무원/지원자) 상세 활동, 급여 안내

### 학생 데이터 초기화 SQL
- `supabase/migrations/019_reset_all_students.sql` — 교사 계정 유지, 학생 전체 삭제 + 단계 0 초기화

---

## 2026-06-21

### 실시간 단계 전환 수정 (핵심 버그)
- `useStage` 실시간 구독에서 서버 측 `filter` 제거 → 클라이언트 측 classId 검사로 교체.
  Supabase `postgres_changes` 서버 필터가 묵묵히 실패해 단계 전환이 실시간으로 반영 안 되던 버그 수정.
- `MayorControl` DB 변경 후 `router.refresh()` 추가 — 교사 화면은 실시간에 의존하지 않고 즉시 갱신.
- `useStage` 초기값(initialStage 등) 변경 시 상태 동기화 `useEffect` 추가.

### 교사 컨트롤 UX
- 멈춤/재개 버튼: 현재 상태(✓ 멈춰 있어요) · 로딩(멈추는 중…) · 액션을 명확히 구분.
- 멈춤 설명 문구: "학생 화면이 흐려지고 활동을 못 해요 (설명할 때 집중시키기)" 추가.
- `PausedOverlay` 신규: 교사가 멈추면 학생 화면 전체를 블러 처리 + "잠깐 멈춰요" 안내.

### 시흥시 테스트 반 추가
- DB 마이그레이션 018: 시흥시 (code: 3643410, color: teal) 반 생성
- 학생 로그인 화면: 시흥시 선택 시 "선생님용 테스트 반입니다. 선생님이신가요?" 경고 화면 노출
- 교사 로그인 API 동적화 — 하드코딩 제거, DB에서 code로 class 조회 후 email 반환
- CITY_THEME/CITY_COLORS에 teal 컬러 추가

### 회사 QR 코드 & 자산 흐름
- `CompanyManager`: 판매대 QR 코드 표시 (`company:<companyId>` 인코딩)
- `/company/flow` 신규 페이지: 거래 타임라인(입금 초록/출금 빨강), 유형별 요약

### 품의서 결재 개선
- 교사 품의서 검토 화면: 물건 용도(purpose) 표시 추가, 2열 그리드, 포기항목 사유 표시

### 종합 모니터링 개선
- 접속 상태 필터(접속중/다른화면/미접속) 추가
- 학생 4열·회사 3열 그리드, `max-w-6xl` 확장

### 역할 스토리 카드(RevealWatcher) 개선
- CEO/직원/공무원 전직 팝업: 역할 스토리·할 일 목록·팁 풀 카드로 확장

## 2026-06-19

### 보안·완결성
- **전 활동 페이지 보드 게이트** — `lib/guard.ts` + `ActivityLocked`로 12개 활동 페이지 모두 보드 미열림 시 URL 직접접근 차단.
- **학생 피드백 표시** — `FeedbackBanner`로 사업계획서·도시탐구·성찰 화면에 교사 피드백 노출.

### 교사 제출물·피드백
- **학생 제출물 구역** (`/admin/submissions`) — 사업계획서·도시탐구·성찰을 탭으로 모아 보고, 각 제출물에 피드백 작성·저장.
- 011 마이그레이션: business_plans·city_research·reflections에 feedback 컬럼 + 교사 update 정책.

### 교사 수업 보드 (큰 개편)
- **교사 수업 보드** — 활동을 켜고/끄고, 드래그로 순서 배열. 단계(0~4) 대신 활동 단위로 통제.
- **학생 홈** — 교사가 켠 활동만, 짠 순서대로 실시간 표시. 이전 활동을 켜두면 미완 학생이 이어서 마무리 가능.
- **활동 페이지** — 단계 잠금 제거, 보드로 열린 동안 작업 가능. explore·plan은 URL 직접접근 게이트까지 적용.

### UX
- **학생 홈 개편** — 현재 단계 작업을 도시 컬러 큰 카드로 부각, 이전 단계는 작게, 미래(잠긴) 작업은 숨김. "지금 할 일"이 한눈에.

### 운영·관리
- **슈퍼어드민** 추가 (`/admin/super`) — 반별 시장 현황·해제, 잘못 등록된 교사 계정 정리.
  `SUPER_ADMIN_EMAIL` 환경변수로 지정된 교사만 접근.
- **시장 등록 친절한 안내** — 이미 시장이 있는 반에 등록 시 명확한 한글 메시지(중복키 에러 대신).
- 루트(`/`) 페이지가 기본 Next 템플릿이던 버그 → `/home` 리다이렉트로 수정.

### 협업 체계
- **CI 추가** (`.github/workflows/ci.yml`) — PR·push 시 자동 타입체크+빌드. "의미 충돌"(삭제된 함수 호출 등) 검출.
- **협업 가이드** (`docs/협업가이드.md`) — 브랜치 흐름, 충돌·합치기 순서, DB 동기화, main=실서비스 규칙.

### 배포·성능
- **Vercel 배포** (gyeonggi-startup.vercel.app), 함수 리전 서울(`icn1`) 고정으로 속도 개선.
- **keep-alive** GitHub Actions — 3일마다 Supabase ping(7일 정지 방지). 동작 확인 완료.
- 학생 홈 카드를 "오늘 할 일 / 언제든지 해요" 두 섹션으로 정리.

### AI 연속성 문서
- `docs/ARCHITECTURE.md`(코드 구조·패턴·함정), `docs/DECISIONS.md`(결정 배경) 추가.

## 2026-06-18

### 전 기능 구현 (빌드 우선순위 1~10)
- 도시 탐구(0단계) + 성찰 + 공용 컴포넌트(PageShell·ConceptPopup·TaskCard 등)
- GRASPS 인트로 슬라이드 + 역할 전직 팝업(CEO 로그인 / 직원·공무원 Realtime)
- 창업: 사업계획서 + 교사 선정 + 지원금 멱등 지급 + CEO 전직
- 회사 설립 + 직원 채용(CEO) + 공무원 임명(교사) + 품의서·결재
- 생산: 업무일지 + 급여(멱등) + 시설 관리·이용 + 공무원 시찰 보고서 + 거래장부
- 판매 QR 결제(체크카드): 자가구매·재고·잔액·멱등 방어
- 교류·박람회(돈 거래 없는 협력 기록)
- 교사 현황 보드 + 쪽지시험 3회 + 형성평가 5종

### 기반
- DB 마이그레이션 001~009 (테이블·RLS·realtime). RLS 재귀는 SECURITY DEFINER 헬퍼로 해결.
- 로그인(반→번호→PIN), 최초 PIN 강제 변경, 4학년 친근형 디자인 톤 확정.
- 멱등 이체 헬퍼(`lib/ledger.ts`), PIN 6자 우회(`pinToPassword`).
- 프로젝트 초기화: Next.js + Supabase + Vercel + GitHub, AI 컨텍스트 문서(CLAUDE.md 등).
